PORT=8787
CLIENT_ORIGIN=http://localhost:5173
GEMINI_API_KEY=
# Optional override — default uses a fast general model
# GEMINI_MODEL=gemini-2.0-flash

# Optional: Firebase Admin service account JSON (single line) for verifying Bearer ID tokens.
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# When true, requires a valid Firebase ID token (Bearer) when Admin is initialized.
# STRICT_AUTH=false
